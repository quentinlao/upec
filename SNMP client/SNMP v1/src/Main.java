
import java.io.IOException;
import org.ietf.jgss.GSSException;




public class Main {

	public static void main(String[] args) throws IOException, GSSException {

		
		
		Fenetre fen = new Fenetre();
		/*
	
		byte[] packEncode = encode.packetEncode();
		DatagramPacket packSortant = new DatagramPacket(packEncode,packEncode.length, encode.getIp(), 161);

		DatagramSocket socket = new DatagramSocket();
		socket.connect(encode.getIp(), 161);
		socket.send(packSortant);
		
		byte[] buff = new byte[1000];
		DatagramPacket packEntrant = new DatagramPacket(buff, buff.length);
		
		System.out.println("PAS ENCORE");
		socket.receive(packEntrant);
		System.out.println("FINI");*/
		
		
		
		
	}

}
